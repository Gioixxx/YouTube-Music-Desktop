{
  "name": "youtube-music-desktop",
  "version": "1.1.0",
  "description": "YouTube Music Desktop App built with Electron",
  "main": "app/main/main.js",
  "scripts": {
    "start": "electron .",
    "build": "echo 'Build check OK' && exit 0",
    "dist": "electron-builder --win",
    "release": "npm run release:stable",
    "release:stable": "electron-builder --win --publish always -c.publish.releaseType=release",
    "release:prerelease": "electron-builder --win --publish always -c.publish.releaseType=prerelease"
  },
  "keywords": [
    "youtube",
    "music",
    "electron",
    "desktop"
  ],
  "author": "",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "discord-rpc": "^4.0.1",
    "electron-store": "^8.1.0",
    "electron-updater": "^6.8.3"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "build": {
    "appId": "com.youtube-music-desktop.app",
    "productName": "YouTube Music Desktop",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icons/icon.ico",
      "publisherName": "Gioixxx"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "artifactName": "YouTubeMusicDesktopSetup.exe",
      "installerIcon": "assets/icons/icon.ico",
      "uninstallerIcon": "assets/icons/icon.ico",
      "installerHeaderIcon": "assets/icons/icon.ico",
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "portable": {
      "artifactName": "YouTubeMusicDesktop.exe"
    },
    "publish": {
      "provider": "github",
      "owner": "Gioixxx",
      "repo": "YouTube-Music-Desktop",
      "releaseType": "release"
    },
    "files": [
      "app/**/*",
      "assets/**/*",
      "config/**/*",
      "package.json"
    ]
  }
}

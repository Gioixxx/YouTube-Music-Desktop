// Default application configuration values.
// These are the baseline settings used when no user config exists.

module.exports = {
  theme: 'system',
  startMinimized: false,
  minimizeToTray: true,
  closeToTray: true,
  showNotifications: true,
  enableGlobalShortcuts: true,
  enableDiscordRPC: false,
  adBlockUI: true,
  window: {
    width: 1280,
    height: 800,
  },
};
